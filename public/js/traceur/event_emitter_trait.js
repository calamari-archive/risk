trait EventEmitterTrait {
  function fire(type, parameters) {
//    this._eventEmitterObject = this._eventEmitterObject || $('<b></b>');
//    this._eventEmitterObject.trigger('this.' + type, parameters || []);
  }
  
  function on(type, handler) {
//    this._eventEmitterObject = this._eventEmitterObject || $('<b></b>');
//    this._eventEmitterObject.bind('this.' + type, handler);
  }
}
